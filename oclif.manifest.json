{
  "version": "1.0.0",
  "commands": {
    "metadata:adjust": {
      "id": "metadata:adjust", 
      "pluginName": "sf-metadata-adjust",
      "pluginType": "core",
      "summary": "Sort and format Salesforce metadata XML files",
      "description": "Recursively processes all *-meta.xml files in the specified directory, sorting XML elements alphabetically with special handling for Salesforce metadata structures like PermissionSets, Profiles, and other metadata types.",
      "examples": [
        "sf metadata adjust",
        "sf metadata adjust ./force-app/main/default",
        "sf metadata adjust --no-backup",
        "sf metadata adjust ./src/main/default --no-backup"
      ],
      "flags": {
        "target-dir": {
          "name": "target-dir",
          "type": "option",
          "char": "d",
          "summary": "Target directory to process",
          "description": "Directory containing Salesforce metadata files to process. Defaults to current directory.",
          "required": false,
          "default": "."
        },
        "no-backup": {
          "name": "no-backup", 
          "type": "boolean",
          "summary": "Skip creating backup before processing",
          "description": "When specified, the command will not create a backup directory before processing files.",
          "allowNo": false,
          "default": false
        }
      },
      "args": {
        "path": {
          "name": "path",
          "description": "Path to the directory containing metadata files to process",
          "required": false
        }
      }
    }
  }
}